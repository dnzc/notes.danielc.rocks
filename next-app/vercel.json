{
  "buildCommand": "cd ../ssg && python3 -m pip install -r requirements.txt --break-system-packages && python3 run.py && cd ../next-app && npm run build",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- ../ssg ../source ./",
  "git": {
    "deploymentEnabled": {
      "dev": false
    }
  }
}